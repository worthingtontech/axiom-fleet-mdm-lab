<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>PayloadType</key>
  <string>Configuration
  <key>PayloadDisplayName</key>
  <!-- DELIBERATELY MALFORMED: the <string> above is never closed, so plistlib
       must fail to parse this. Used once to prove the profiles gate fails CI,
       then removed. -->
</dict>
</plist>
